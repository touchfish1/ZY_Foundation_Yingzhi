# Placeholder secret templates — actual values must be set via:
#   kubectl create secret generic zhangyuan-db \
#     --from-literal=host=... \
#     --from-literal=password=...
#
#   kubectl create secret generic zhangyuan-auth \
#     --from-literal=jwt-secret=... \
#     --from-literal=admin-password=...
#
#   kubectl create secret generic zhangyuan-redis \
#     --from-literal=host=...
#
#   kubectl create secret generic zhangyuan-nacos \
#     --from-literal=password=...
#
#   kubectl create secret generic zhangyuan-minio \
#     --from-literal=endpoint=http://minio:9000 \
#     --from-literal=access-key=zhangyuan \
#     --from-literal=secret-key=...
#
#   kubectl create secret generic zhangyuan-ai \
#     --from-literal=openai-api-key=... \
#     --from-literal=claude-api-key=... \
#     --from-literal=deepseek-api-key=... \
#     --from-literal=qwen-api-key=... \
#     --from-literal=moonshot-api-key=...
